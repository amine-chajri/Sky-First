services:
  mongo:
    image: mongo:7
    container_name: sky-first-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - sky_first_mongo_data:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  sky_first_mongo_data: