# Sky First frontend environment
# Base URL of the deployed server API (Vercel project with server/ as root).
# Optional: when unset, the client uses the same-origin "/api" path (local dev via the Vite proxy).
VITE_API_URL=https://your-server.vercel.app